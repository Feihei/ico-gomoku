# AGENTS.md — 球面五子棋

> 本文档面向 AI Agent 与人类协作者，是本项目的**唯一权威规范**。开始任何编码任务前必须通读本文档。

---

## 1. 项目概述

**球面五子棋**：将传统五子棋棋盘从二维平面迁移到球面流形上。棋盘采用正二十面体细分生成的球形三角网格，棋子落在顶点上，任意一方在球面上沿网格边连成 N 子一线即获胜。

- **技术栈**：Three.js + HTML + CSS + 原生 JavaScript（ES Modules），无构建工具，无框架
- **运行方式**：静态页面，任意静态服务器（如 `python -m http.server` 或 VS Code Live Server）打开即玩
- **目标**：有限、无边界、可循环的棋盘，消除平面五子棋的边角效应，降低和棋率

## 2. 核心数学规范

### 2.1 网格生成：Icosphere 细分

- 从正二十面体（12 顶点、30 边、20 面）出发，做频率 n 的边细分（每个大三角形分为 n² 个小三角形），所有新顶点投影回单位球面
- **顶点数 V = 10n² + 2，边数 E = 30n²，面数 F = 20n²**
- 默认参数：`n = 5`（V = 252），必须可通过配置常量调整
- 细分算法建议：对二十面体每条边按频率插入点后做球面投影（比递归中点细分更适合任意 n），或使用递归中点细分限制 n 为 2 的幂。**实现时二选一并保持一致**

### 2.2 度数奇点（关键约束）

由欧拉公式，球面三角网格**必然恰好有 12 个度数为 5 的顶点**（即二十面体原始顶点），其余顶点度数为 6。

- 这 12 个顶点定义为 **"星位点"**
- 默认规则：星位点**可正常落子**，但其连珠方向按实际邻接数处理（见 2.3）
- 必须在渲染上对星位点做视觉标记（如金色环）
- 网格初始化后必须断言：`度数为5的顶点数 === 12`，否则抛错

### 2.3 邻接与连珠判定

- 每个顶点维护邻接表：度数为 6（三条轴向，每轴 2 个方向）或 5（五价点，方向轴不完整）
- **连珠判定算法**：对每个已落子顶点，枚举其所有轴向（度数 6 → 3 轴，度数 5 → 每对相邻边作为候选方向），沿每个方向双向延伸统计连续同色子数，`>= N` 即胜
- **五价点规则**：从五价点出发的连珠，可沿其 5 个邻居中任意方向延伸；穿过五价点继续连珠时，要求进入边与离开边在网格上构成"近直线"（两边的顶点转角最小者），实现时以"进入方向对应的出边取转角最小的一条"为准
- 默认获胜连珠数 `WIN_N = 5`（v1 固定为 5，无 UI 入口，勿改）

### 2.4 平局判定

- 所有顶点落满且无 N 连珠 → 和棋
- 局面重复不构成和棋（本网格无平移对称性，无循环争议）

## 3. 游戏规则规范

1. 黑先白后，交替落子于未被占据的顶点
2. 落子后立即执行连珠检测，命中即结束
3. 落子合法性与胜负判定**必须基于邻接图（组合结构）**，不得依赖三维坐标距离
4. 无禁手、无交换规则（v1 简化版）；代码结构需为后续加入 Pie Rule（交换规则）预留接口

## 4. 技术架构与文件结构

```
spherical-gomoku/
├── index.html          # 入口，DOM 骨架
├── css/
│   └── style.css       # UI 样式
├── js/
│   ├── main.js         # 入口：初始化渲染循环、装配各模块
│   ├── config.js       # 所有可调常量（细分频率、胜利连珠数、颜色、相机参数）
│   ├── mesh/
│   │   ├── icosphere.js    # 网格生成：顶点、面、邻接表构建
│   │   └── validate.js     # 网格校验（V/E/F 公式、12 个五价点、邻接对称性、面绕序朝外）
│   ├── game/
│   │   ├── board.js        # 棋局状态：落子、悔棋、查询
│   │   ├── rules.js        # 合法性判定、连珠检测、胜负/平局判定
│   │   └── history.js      # 手数记录（支持悔棋与复盘）
│   ├── render/
│   │   ├── scene.js        # Three.js 场景、灯光、相机、OrbitControls
│   │   ├── boardView.js    # 网格线、顶点标记、星位点渲染
│   │   └── stoneView.js    # 棋子网格池（实例化渲染）、落子动画、胜利连线高亮
│   └── interaction/
│       └── raycaster.js    # 拾取：鼠标→最近可落子顶点、悬停预览
└── tests/
    ├── mesh.test.html      # 浏览器内运行的结构校验页面
    ├── rules.test.html     # 规则单元测试页面
    └── run.mjs             # 无头运行器：node tests/run.mjs（Node 中提取 HTML 页内脚本执行，供 CI/快速验证）
```

**依赖引入**：ES Module + `importmap`，`three` 与 `OrbitControls` 以本地文件形式存放于 `js/vendor/`（保证离线可玩；如换 CDN 需同步改 `index.html` 的 importmap）。**禁止引入 Node 专属 API、打包器或 npm 依赖**——本项目必须在纯静态环境下运行。

**测试约定**：不引入测试框架，使用极简自写断言（`assert(cond, msg)`），以 HTML 页面承载、控制台输出结果。测试可独立打开运行；也可用 `node tests/run.mjs` 无头运行同一套断言（Node ≥ 22 对 `.mjs` 原生按 ESM 处理，项目无 `package.json`）。

## 5. 模块职责与接口约定

### 5.1 `mesh/icosphere.js`

```js
// 返回结构，落子逻辑只依赖 adjacency，不依赖 positions
{
  positions: Float32Array,   // 长度 3V，单位球面上的坐标
  adjacency: number[][],     // V 个数组，每个为该顶点邻居索引（升序）
  faces: number[][],         // 三角面片（仅渲染用）
  degree5Vertices: number[]  // 12 个五价点索引
}
```

- 邻接表必须**对称**（i 在 j 的邻接中 ⟺ j 在 i 的邻接中），`validate.js` 校验

### 5.2 `game/rules.js`

```js
checkWin(board, vertexIndex, player) // → { win: boolean, line: number[] | null }
```

- `line` 返回获胜连珠的顶点索引序列，用于高亮渲染
- 此函数**必须为纯函数**，不持有状态

### 5.3 状态管理

- `board.js` 是唯一的棋局状态持有者；渲染层只读
- 每次状态变更通过显式调用触发渲染更新，不使用观察者框架

### 5.4 交互流程

1. Raycaster 命中棋子网格或顶点标记 → 换算为最近顶点索引
2. 悬停时显示半透明预览棋子（当前执子方颜色）
3. 点击 → `board.place()` → `rules.checkWin()` → 更新渲染或触发胜利流程
4. 棋盘必须支持鼠标拖拽旋转、滚轮缩放（OrbitControls）

## 6. 渲染规范

**总体风格：纸面印刷风（非真实渲染）**。全部几何用不受光的 `MeshBasicMaterial`/`LineBasicMaterial`，场景**不设灯光**；底色与球面共用同一纸色 `COLOR_PAPER`（页底 = 纸面实体 = 白子填充，三者一致），**物体本身一律纯平涂色、不带贴图噪波**；纸纤维质感由**最上层全视口噪波覆盖层**（`#paper-noise`，`main.js` 的 `paintNoiseOverlay()` 生成灰度颗粒图块平铺，强度 `PAPER_NOISE_STRENGTH`）统一叠加——保证背景/球面/白子/UI 颗粒缩放一致。墨色元素见 `config.js` 的 `COLOR_*`。

- **背面遮挡**：纸面实体 = **复用同一细分 icosphere 的三角面片**（`OCCLUDER_SCALE` 0.99 微缩），且**所有面必须统一朝外绕序**（`validate.js` 断言），否则 FrontSide 渲染会把朝内的面当背面剔除、形成破洞透出背面网格；与网格线同拓扑，每根网格线都严格位于其外侧（无 z-fight、正面线不会被"额外更细的球"吃掉），同时自身不透明，天然把背面网格与棋子遮挡掉。**禁止**另加更细分的圆球做遮挡（细分不一致会吞正面线）
- 网格线：墨色细线（`LineSegments`，`COLOR_LINE`）；**普通顶点不画点**，交叉处只由线段本身构成
- 星位点：实心小墨点（`STAR_MARK_RADIUS`，普通交叉点小点样式，`COLOR_STAR`），仅标记 12 个五价点；不做圆环；**几何沿法向压扁 `STONE_SQUASH`**（与棋子一致，逐实例定向贴服球面）
- 棋子（均用 `InstancedMesh`，252+ 子性能）：
  - **扁球几何**：球体沿局部 +Z 压扁 `STONE_SQUASH`=0.5（高度为半径方向一半），逐实例用四元数把 +Z 对齐到顶点球面法向
  - 底面贴临纸面实体：中心位于顶点球面半径 + `STONE_Z_OFFSET` 处
  - 黑子：墨色扁球（`COLOR_INK`）；白子：纸色扁球（填充 = 背景纸色，落下后遮住下方棋盘格点）
  - **描边壳**：黑/白子均外覆一层略大的墨壳（BackSide 渲染，半径 + `STONE_OUTLINE`）——白子呈现黑描边，黑子同壳保持外轮廓一致，避免白子显得更大
- 悬停预览：半透明墨色，黑执子显示半透明墨扁球、白执子显示半透明墨圈，浮于目标顶点
- 胜利连线：将获胜顶点序列沿球面用**细墨管**（TubeGeometry，`COLOR_WIN`，半径 0.008）连接，线贴近网格线层（压在棋子底下、从棋子之间穿过）；**其它棋子不再缩小**（高亮只靠细线表达）
- 性能目标：60 FPS，无 GC 明显卡顿；几何体与材质须复用（重建时随 geometry 一并 dispose）

## 7. UI 规范

- **布局**：左侧竖向工具 bar（`#hud`），收起按钮 **✕ 置最左上角**（点击收起，折叠后仅剩左上角 ☰ 按钮可再展开），其余区域完全展示棋盘（canvas 全幅）
- bar 内容（竖排按钮组，信息区在下）：
  - **模式切换**（`#btn-mode` 对战/人机）：v1 无 AI，人机为 **UI 占位**——点选后提示"人机 AI 开发中"并自动回退对战
  - **细分频率 5/6 两档切换**（`#freq-group` 内两个按钮，选中档墨色反白；只有 5/6 两档）；切换重建棋盘会清空对局，需二次确认
  - 新对局、悔棋、**导出棋局 JSON**（下载文件：细分频率、连珠数、全部手数、结果）
  - 信息区：手数、执子方、消息（**无"对局开始，黑先"式初始消息**，默认留空）
- 获胜连珠数**固定 5**，无 UI 入口
- **结束状态**：胜利/和棋时在**视口中央弹出大字**（`#win-overlay`："黑胜" / "白胜" / "和棋"，印刷风双线框大字），点新对局/悔棋/重建即消除
- **风格**：全部 UI 为 HTML/CSS 覆盖层（纸底墨字印刷风），与 WebGL canvas 分层；**禁止在 WebGL 内绘制文字 UI**；文字、按钮、下拉框一律纸面底 + 墨色文字/描边，禁止玻璃拟态与彩色强调

## 8. 编码约定

- ES Modules，`const` 优先，无全局变量污染
- 所有常量集中在 `config.js`，禁止魔法数字散落各处
- 注释语言：中文；注释解释"为什么"而非"是什么"
- 网格相关代码必须通过 `validate.js` 全量校验后才允许进入游戏逻辑（启动时自动执行）
- 禁止使用 `any` 类思维的全局 try-catch 吞错；错误直接抛出并在控制台给出上下文

## 9. 实施里程碑（按序执行）

1. **M1 网格生成**：icosphere 生成 + 邻接表 + 校验器，测试页输出 V/E/F 与五价点统计
2. **M2 静态渲染**：网格可视化、星位点标记、OrbitControls
3. **M3 交互与落子**：Raycaster 拾取、悬停预览、落子渲染
4. **M4 规则引擎**：合法性、连珠判定（含五价点穿行规则）、胜负与平局、测试页覆盖边界用例
5. **M5 完整对局**：手数记录、悔棋、胜利高亮、UI 完整化
6. **M6 打磨**：落子动画、音效占位、参数化配置面板

每个里程碑完成后，运行对应测试页并确认控制台零报错再进入下一个。

## 10. 已知设计决策（勿擅自更改）

- 细分算法选定 **§2.1 的"频率格点法"（重心坐标撒点 + 球面投影）**，支持任意 n；已实现，勿改回递归中点细分
- 落子点为**顶点**而非对偶面心：实现更简单，组合结构等价
- 连珠判定基于**轴向延伸**而非任意路径：与平面五子棋语义一致
- 五价点可落子：保留其作为战略要点的游戏性
- v1 不做 AI 对手、不做联机：结构上预留 `board.js` 的状态快照接口以便未来扩展

## 11. 给 Agent 的工作守则

- 修改规则引擎前，先阅读 §2.3 与 §2.4，任何对五价点连珠语义的调整必须在本文档同步更新
- 不确定设计意图时，以本文档为准；文档未覆盖的细节，选择**与平面五子棋语义最接近**的方案并在此登记
- 每次提交保证：静态服务器打开即玩、测试页通过、控制台无错误
